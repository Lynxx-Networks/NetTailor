vlan {vlan-number}
 name {vlan-name}

interface Vlan{vlan-number}
 description {vlan-name}
 ip address {vlan100-ip} {vlan100-subnet}
 ip helper-address {dhcp-server1}
 ip helper-address {dhcp-server2}